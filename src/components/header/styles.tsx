import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  login: {
    marginRight: 15,

    fontWeight: 'bold',
    color: '#FFF',
  },
  image: {
    width: 20,
    height: 20,
    marginRight: 20,
  },
})

export default styles
